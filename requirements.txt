# API service dependencies, pinned to the versions this submission was built
# and evaluated against (a reviewer building weeks later gets the same stack).
# torch is installed separately in Dockerfile.api from the CPU wheel index to
# keep the image CPU-only and small. Dev/test tools live in requirements-dev.txt.
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32
redis==8.0.1
chromadb==1.5.9  # must match the chromadb/chroma server image tag in docker-compose.yml
litellm==1.91.1
python-jose[cryptography]==3.5.0
PyMuPDF==1.28.0
sentence-transformers==5.6.0
transformers==5.13.0
rank-bm25==0.2.2
httpx==0.28.1
slowapi==0.1.10
prometheus-client==0.25.0
